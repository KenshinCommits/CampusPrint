# Local dev default - points at the backend running on your machine.
# When you deploy the backend to ECS, change this to that service's
# Application URL (see Deployment_Guide_1.pdf Section 9) and rebuild.
VITE_API_URL=http://localhost:4000
